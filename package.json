{
  "name": "tokenizer",
  "private": true,
  "scripts": {
    "prestart": "npm install --silent",
    "minify": "node run/minify",
    "start": "node run/start",
    "pretest": "npm install --silent",
    "test": "node run/test",
    "test:tokenizer": "node run/test",
    "test:parser": "node run/test-parser"
  },
  "devDependencies": {
    "benchmark": "2.1.4",
    "bootstrap": "4.5.0",
    "postcss": "7.0.30",
    "postcss-selector-parser": "^6.0.2",
    "postcss-value-parser": "^4.1.0",
    "postcss-values-parser": "^3.2.1",
    "terser": "^4.6.13"
  }
}
