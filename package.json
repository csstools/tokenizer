{
  "name": "tokenizer",
  "private": true,
  "scripts": {
    "prestart": "npm install --silent",
    "minify": "node run/minify",
    "start": "node run/start",
    "pretest": "npm install --silent",
    "test": "node run/test"
  },
  "devDependencies": {
    "benchmark": "2.1.4",
    "bootstrap": "4.4.1",
    "postcss": "7.0.27",
    "terser": "^4.6.12"
  }
}
